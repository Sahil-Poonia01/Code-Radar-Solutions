#include <stdio.h>

int main() {
    float num;  // Variable to store the floating-point number

    // Taking floating point input from the user

    scanf("%f", &num);

    // Printing the floating point number with 2 decimal places
    printf("You entered: %.2f\n", num);

    return 0;
}
