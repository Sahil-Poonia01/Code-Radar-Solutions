#include <stdio.h>

int main() {
    char word[100]; // Array to store the word (max 99 characters)

    
    // Taking input using scanf
    scanf("%s", word);

    // Printing the word using printf
    printf("You entered: %s\n", word);

    return 0;
}
